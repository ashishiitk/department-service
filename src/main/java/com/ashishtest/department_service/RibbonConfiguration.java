package com.ashishtest.department_service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;



@Configuration
public class RibbonConfiguration {

	/*
	 * @Autowired(required = true) public IClientConfig iClientConfig;
	 * 
	 * public RibbonConfiguration(IClientConfig iClientConfig) { iClientConfig =
	 * iClientConfig; }
	 * 
	 * @Bean public IPing ribbonIPing(IClientConfig iclientConfig) { return new
	 * PingUrl(); }
	 * 
	 * @Bean public IRule ribbonIrule(IClientConfig iClientConfig) { return new
	 * AvailabilityFilteringRule();
	 * 
	 * }
	 */
}
